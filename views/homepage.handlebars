{{#each posts as |post|}}
<div style="border: 2px inset black; border-radius: 15px; margin: 8px; padding: 4px; padding-left:15px">
    <a href='/{{id}}'><h2>{{title}}</h2></a>
    <h3>Posted by {{post.user.username}} on {{createdAt}}</h3>
    <p>{{post_content}}</p>
</div>
{{/each}}